import React,{useEffect,useState}from"react";import API from"../services/api";function Variables(){const[vars,setVars]=useState([]);useEffect(()=>{API.get("/variables").then(r=>setVars(r.data))},[]);return <div><h1>Variables Used</h1><p>Variables included in prediction and backend API.</p><div className="cards">{vars.map((v,i)=><div className="card small" key={i}>{v}</div>)}</div></div>}export default Variables;
